# Server-side API gateway for the Python Agent service.
# Keep database URLs, service keys and provider keys on the server only.
REVENUEOPS_API_BASE_URL=https://api.example.com

# Exposed only as a non-sensitive release label in the interface.
NEXT_PUBLIC_APP_ENV=production
