# Public HTTPS endpoint for the Python Agent API. This contains no secret.
# Leave blank during demo mode; set it in Cloudflare Pages before production rebuild.
NEXT_PUBLIC_REVENUEOPS_API_URL=https://api.example.com

# Exposed only as a non-sensitive release label in the interface.
NEXT_PUBLIC_APP_ENV=production
